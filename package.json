{
  "name": "inventory-management-system",
  "version": "1.0.0",
  "description": "Full-stack Inventory Management System (React + Node.js + MongoDB)",
  "scripts": {
    "start": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "seed": "npm run seed --prefix server",
    "test:api": "node server/test_api.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
